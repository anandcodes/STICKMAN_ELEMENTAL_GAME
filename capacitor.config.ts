import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stickman.elemental',
  appName: 'Stickman Elemental',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
